/*
This project is a Caesar cipher program written in C++ that performs encryption and decryption by shifting letters based on a rotation value. It allows users to input text and either encode or decode messages using different cipher functions. The project was built as part of a CS251 assignment to practice object-oriented programming, string manipulation, and modular design. I structured the program into multiple functions and headers to keep the code organized and reusable. I also implemented a full testing suite using Google Test to verify correctness of functions like rotation, cleaning input, splitting words, joining strings, and dictionary-based decryption.

During development, I used automated tests to catch edge cases such as empty inputs, multiple spaces, invalid characters, and incorrect vector modifications. I fixed bugs related to string indexing, improper space handling, and incorrect rotation logic. Testing helped me confirm that functions behaved correctly before integrating them into the main program. I also used debugging tools and iterative testing to ensure the decrypt command worked correctly with dictionary matching and output formatting. Overall, the project improved my understanding of software testing, debugging, and structured program design.

Tools Used

• C++ programming language
• Google Test / Google Mock for unit testing
• VS Code for development
• Terminal for compiling and running tests
• Git for pushing the final version/future revisions
*/
